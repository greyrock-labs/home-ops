---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/litellm.home-operations.com/litellmproxy_v1alpha1.json
apiVersion: litellm.home-operations.com/v1alpha1
kind: LiteLLMProxy
metadata:
  name: litellm
spec:
  # renovate: datasource=docker depName=ghcr.io/berriai/litellm
  image: ghcr.io/berriai/litellm:v1.96.0@sha256:90d8de0ea6fbb3cad145d1019d00a0149ae400b1e18e2011a60f1988f143f672
  replicas: 1
  generalSettings:
    health_check_endpoint: /v1/health
  routerSettings:
    routing_strategy: simple-shuffle
    redis_host: os.environ/REDIS_HOST
    redis_port: os.environ/REDIS_PORT
    num_retries: 3
    timeout: 600
    context_window_fallbacks:
      - minimax/MiniMax-M2-7: ["minimax/MiniMax-M3"]
      - minimax/MiniMax-M2-7-anthropic: ["minimax/MiniMax-M3-anthropic"]
      - opencode-go/deepseek-v4-flash: ["opencode-go/qwen3.6-plus", "opencode-go/glm-5.2"]
      - opencode-go/kimi-k2.6: ["opencode-go/glm-5.2"]
      - opencode-go/kimi-k2.7-code: ["opencode-go/glm-5.2"]
      - opencode-go/mimo-v2.5: ["opencode-go/glm-5.2"]
      - opencode-go/qwen3.6-plus: ["opencode-go/glm-5.2"]
      - opencode-go/qwen3.7-max: ["opencode-go/glm-5.2"]
  litellmSettings:
    success_callback:
      - prometheus
    prometheus_adapter: true
    cache: true
    cache_params:
      type: redis
      host: os.environ/REDIS_HOST
      port: os.environ/REDIS_PORT
    drop_params: true
    merge_reasoning_content_in_choices: true
  env:
    - name: LITELLM_LOG
      value: INFO
    - name: LITELLM_MODE
      value: PRODUCTION
    - name: REDIS_HOST
      value: litellm-dragonfly.ai.svc.cluster.local
    - name: REDIS_PORT
      value: "6379"
    - name: LITELLM_MASTER_KEY
      valueFrom:
        secretKeyRef:
          name: litellm
          key: LITELLM_MASTER_KEY
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 2Gi
  service:
    type: ClusterIP
    port: 4000
  route:
    hostnames:
      - litellm.greyrock.io
    parentRefs:
      - name: envoy-external
        namespace: network
