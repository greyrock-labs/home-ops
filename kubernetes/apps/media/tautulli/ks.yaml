---
# yaml-language-server: $schema=https://k8s-schemas.greyrock.io/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tautulli
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: tautulli
  components:
    - ../../../../components/volsync
  dependsOn:
    - name: plex
      namespace: media
    - name: rook-ceph-cluster
      namespace: rook-ceph
  interval: 1h
  path: "./kubernetes/apps/media/tautulli/app"
  postBuild:
    substitute:
      APP: tautulli
      VOLSYNC_CLAIM: tautulli-config
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  timeout: 5m
  wait: false
