# Images the containers on office-gw should run, one per line as
# "<image>=<tag>". Every container on the router whose remote-image is that
# image gets that tag. Renovate bumps the tag; sync-containers.sh makes the
# router match after the PR is merged.

# renovate: datasource=docker depName=neilpang/acme.sh
neilpang/acme.sh=3.1.6

# renovate: datasource=docker depName=controldns/ctrld
controldns/ctrld=1.5.7
