---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/gateway.envoyproxy.io/backend_v1alpha1.json
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  labels:
    app.kubernetes.io/instance: unleashed
    app.kubernetes.io/name: unleashed
  name: unleashed
spec:
  endpoints:
    - fqdn:
        hostname: unleashed.internal.greyrock.io
        port: 443
  tls:
    insecureSkipVerify: true
